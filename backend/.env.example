# .env.example 파일 내용

# --- AI 모델 설정 ---
# 빠른 피드백용 (우선순위대로 콤마로 나열)
# 이 값은 비밀이 아니므로 그대로 둬도 괜찮습니다.
GEMINI_FAST_MODELS=gemini-2.0-flash-lite,gemini-2.0-flash,gemini-2.5-flash

# 전체 요약용 (우선순위대로 콤마로 나열)
GEMINI_QUALITY_MODELS=gemini-2.5-flash,gemini-2.0-flash

# --- 비밀 키 ---
# !!! (중요) !!!
# 아래 값은 각자 발급받은 Google API 키로 채워야 합니다.
GOOGLE_API_KEY=